export interface Filter {
  minRating: number;
  maxRating: number;
  minYear: number;
  maxYear: number;
  minRuntime: number;
  maxRuntime: number;
  minSeasons: number;
  maxSeasons: number;
  genres: string[];
}
